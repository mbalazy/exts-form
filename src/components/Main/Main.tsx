import { MainStyle, MainWrapper } from "./Main.style";
import { Form } from "../Form/Form";

export const Main = () => {
  return (
    <MainWrapper>
      <MainStyle>
        <Form />
      </MainStyle>
    </MainWrapper>
  );
};
